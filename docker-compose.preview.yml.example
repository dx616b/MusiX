# Optional second instance (port 8081, separate data). See docs/RELEASING.md.
services:
  musix-preview:
    image: dx616b/musix:edge
    ports:
      - "8081:8080"
    environment:
      CONFIG_FILE: /app/config/config.yaml
      MUSIX_SQLITE: /app/data/musicx.db
      PROWLARR_API_KEY: ${PROWLARR_API_KEY:-}
      JACKETT_API_KEY: ${JACKETT_API_KEY:-}
      TRANSMISSION_USER: ${TRANSMISSION_USER:-}
      TRANSMISSION_PASS: ${TRANSMISSION_PASS:-}
    volumes:
      - ./config/config.preview.yaml:/app/config/config.yaml:ro
      - musicx-preview-data:/app/data
    restart: unless-stopped

volumes:
  musicx-preview-data:
