# Copy to .env — optional overrides for docker compose (URLs live in config/config.docker.yaml).
# Defaults match ../streamx: prowlarr.electron.al, streamx.router.al:9117, transmission.electron.al
PROWLARR_API_KEY=
JACKETT_API_KEY=
TRANSMISSION_USER=
TRANSMISSION_PASS=
